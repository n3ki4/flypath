import { getDestinationsList } from "@controllers/destination/get-destinations-list";
import { getDestinationInfo } from "@controllers/destination/get-destination";

export const destinationController = {
  getDestinationsList,
  getDestinationInfo,
};
